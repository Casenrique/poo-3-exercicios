export type DigimonDB = {
    id: string,
    name: string,
    stage: string,
    attribute: string,
    created_at: string
}

export type TamerDB = {
    id: string,
    name: string,
    created_at: string
}

